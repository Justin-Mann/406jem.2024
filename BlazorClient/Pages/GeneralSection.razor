@using BlazorApp.Models

@if ((!string.IsNullOrEmpty(@SectionName)) && @Items != null)
{
    <MudText Typo="Typo.h6" Class="sectionheader">@SectionName</MudText>

    <div class="sectionbody">
        <ul class="fancyList">
            @foreach (var item in Items)
            {
                <li>@item</li>
            }
        </ul>
    </div>
}

@code {
    [Parameter]
    public string? SectionName { get; set; }
    [Parameter]
    public IEnumerable<string>? Items { get; set; }
}
